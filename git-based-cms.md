---
type: article
slug: git-based-cms
title: "Git-based CMS: контент как код"
description: "Почему Git-workflow для контента работает лучше админок."
hub:
  - static
  - deployment
order: 2
related:
  - "[[deployment-github-actions]]"
  - "[[rules-yaml-basics]]"
---
# Git-based CMS: контент как код

Классические CMS предлагают админку.  
Git-based CMS предлагают **модель**.

Вместо вопроса  
> «Как редактировать контент?»  

они отвечают на другой:  
> **«Как контент живёт, версионируется и развивается во времени?»**

---

## Что такое Git-based CMS

Git-based CMS — это подход, при котором:

- контент хранится в Git-репозитории
- основной формат — Markdown
- изменения фиксируются коммитами
- публикация происходит через сборку
- Git — единственный источник истины

Нет базы данных.  
Нет браузерной админки.  
Нет скрытой логики.

---

## Почему админка — не всегда плюс

Админки хороши для быстрого старта,  
но при росте они создают проблемы:

- сложно отслеживать изменения
- нет прозрачной истории
- трудно масштабировать команду
- контент живёт отдельно от структуры

В результате:
- структура расползается
- качество падает
- поддержка дорожает

---

## Контент как код — ключевой сдвиг

Когда контент — это код:

- у него есть история изменений
- есть review и контроль качества
- есть единые правила
- есть воспроизводимость

Markdown-файл:
- проще шаблона
- стабильнее записи в БД
- долговечнее UI-формы

---

## Git как инфраструктура доверия

Git даёт то, чего нет в CMS:

- diff — видно, что изменилось
- blame — понятно, почему
- branch — можно экспериментировать
- rollback — можно откатиться

Для контента это критично,  
особенно в SEO- и knowledge-проектах.

---

## Масштабирование команды

В Git-based CMS:

- авторы пишут Markdown
- редакторы делают review
- архитектура проверяется автоматически
- деплой — результат merge

Команда масштабируется **без хаоса**.

---

## Связь с архитектурой сущностей

Git-based CMS особенно хорошо ложится на модель:

- сущности
- связи
- хабы
- правила

Контент не просто хранится —  
он **встраивается в систему**.

👉 Архитектура: [[entities-and-links]]]

---

## SEO и Git-based подход

Для SEO важно:

- стабильность URL
- предсказуемость структуры
- контроль изменений
- чистый HTML

Git-based CMS:
- исключает случайные правки
- фиксирует структуру
- делает изменения осознанными

SEO становится **следствием процесса**, а не ручной оптимизации.

👉 Подробнее: [[seo-for-static-sites]]

---

## Obsidian → Git → прод

На практике workflow выглядит так:

1. Автор пишет в Obsidian
2. Markdown синхронизируется с Git
3. Коммит → pull request
4. CI собирает сайт
5. Готовый HTML деплоится

Без серверов.  
Без админки.  
Без ручных действий.

👉 Практика: [[deployment-obsidian-git-prod]]

---

## Где здесь Notepub

[[notepub-seo-friendly-websites-from-markdown-without-cms|Notepub]] строится именно вокруг Git-based подхода:

- Markdown — основной формат
- структура описывается декларативно
- Git — источник истины
- сборка — воспроизводима

Notepub не «заменяет CMS».  
Он убирает необходимость в ней.

---

## Когда Git-based CMS не подойдёт

Важно честно сказать:

- если контент редактируют нетехнические пользователи
- если нужен визуальный WYSIWYG
- если контент создаётся пользователями в реальном времени

В этих случаях нужен другой инструмент  
или гибридная архитектура.

---

## Итог

Git-based CMS — это не про удобство кликов.  
Это про **устойчивость системы**.

Если контент:
- ценен,
- растёт,
- живёт годами,

он заслуживает архитектуры уровня кода.

Именно поэтому Git становится лучшей CMS  
для современных контентных систем.

## Дальше по теме
- Туда: [[deployment-github-actions]]
- И сюда: [[rules-yaml-basics]]

Связанные хабы: [[static]], [[deployment]]
